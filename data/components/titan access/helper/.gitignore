# MIDL-generated (regenerated by build.bat) and MSVC intermediates.
nvdaController.h
nvdaController_s.c
nvdaController_c.c
dlldata.c
*.obj
*.lib
*.exp
*.pdb
# Keep: the built products (titan_access_helper.dll, nvda_probe32.exe,
# nvda_probe64.exe -- the probes are how the bridge is verified per bitness on a
# machine with no compiler), plus *.c, *.idl, build.bat, README.md
